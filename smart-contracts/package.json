{
  "name": "smart-contracts",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "hardhat clean",
    "compile": "hardhat compile",
    "build": "npm run compile"
  },
  "devDependencies": {
    "@openzeppelin/contracts": "^5.4.0",
    "dotenv": "^16.4.5",
    "hardhat": "^3.0.5"
  },
  "dependencies": {
    "ethers": "^6.15.0"
  }
}
